# SmartPayUT API Gateway

Gateway reactivo del MVP. En la Fase 1 solo incluye arranque, Actuator y seguridad JWT base. Las rutas funcionales se incorporarán en una fase posterior sin modificar el gateway legacy.
