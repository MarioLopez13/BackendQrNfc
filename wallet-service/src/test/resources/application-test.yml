spring:
  datasource:
    url: jdbc:h2:mem:wallet;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
  flyway.enabled: true
  jpa.hibernate.ddl-auto: validate
  rabbitmq.listener.simple.auto-startup: false
  security.oauth2.resourceserver.jwt.jwk-set-uri: http://localhost:8180/realms/smartpayut/protocol/openid-connect/certs
